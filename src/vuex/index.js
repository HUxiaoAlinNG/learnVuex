import { Store, install, mapGetters, mapActions, mapMutations, mapState } from "./store";

export default {
  Store,
  install,
}

export {
  Store,
  install,
  mapGetters,
  mapActions,
  mapMutations,
  mapState,
}